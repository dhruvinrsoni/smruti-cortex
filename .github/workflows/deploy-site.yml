name: Deploy Landing Page

on:
  push:
    branches:
      - main
    paths:
      - 'site/**'
      - '.github/workflows/deploy-site.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Configure Git
        run: |
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "actions@github.com"
      
      - name: Backup privacy.html (CRITICAL)
        run: |
          if [ -f docs/privacy.html ]; then
            cp docs/privacy.html /tmp/privacy.html.backup
            cp docs/privacy.html /tmp/privacy.html.backup2
            echo "✅ Privacy policy backed up (2 copies)"
            echo "PRIVACY_BACKUP_EXISTS=true" >> $GITHUB_ENV
          else
            echo "❌ ERROR: No existing privacy.html found"
            echo "PRIVACY_BACKUP_EXISTS=false" >> $GITHUB_ENV
            exit 1
          fi
      
      - name: Clean docs directory (except privacy.html)
        run: |
          if [ -d docs ]; then
            find docs -mindepth 1 ! -name 'privacy.html' -delete
            echo "✅ Cleaned docs directory"
          else
            mkdir -p docs
            echo "✅ Created docs directory"
          fi
      
      - name: Copy site files to docs
        run: |
          cp -r site/* docs/
          echo "✅ Copied site files to docs/"
      
      - name: Restore privacy.html (CRITICAL - NEVER FAIL)
        run: |
          # Try primary backup
          if [ -f /tmp/privacy.html.backup ]; then
            cp /tmp/privacy.html.backup docs/privacy.html
            echo "✅ Privacy policy restored from primary backup"
          # Try secondary backup if primary fails
          elif [ -f /tmp/privacy.html.backup2 ]; then
            cp /tmp/privacy.html.backup2 docs/privacy.html
            echo "⚠️ Privacy policy restored from secondary backup"
          # CRITICAL FAILURE - abort deployment
          else
            echo "❌ CRITICAL: Privacy policy backup failed!"
            echo "🚨 Aborting deployment to prevent privacy URL breakage"
            exit 1
          fi
          echo "🔒 Privacy URL preserved at: docs/privacy.html"
      
      - name: Verify deployment
        run: |
          echo "📁 Deployed files:"
          ls -la docs/
          echo ""
          if [ -f docs/privacy.html ]; then
            echo "✅ Privacy policy verified at docs/privacy.html"
          else
            echo "❌ ERROR: Privacy policy missing!"
            exit 1
          fi
          if [ -f docs/index.html ]; then
            echo "✅ Landing page verified at docs/index.html"
          else
            echo "❌ ERROR: Landing page missing!"
            exit 1
          fi
      
      - name: Commit and push changes
        run: |
          git add docs/
          if git diff --staged --quiet; then
            echo "ℹ️ No changes to deploy"
          else
            git commit -m "🚀 Deploy landing page [skip ci]

            - Updated from site/ directory
            - Privacy policy preserved at docs/privacy.html
            - Deployed by GitHub Actions
            
            Timestamp: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
            
            # Try to push with retry logic
            MAX_RETRIES=3
            RETRY_COUNT=0
            until git push origin main || [ $RETRY_COUNT -eq $MAX_RETRIES ]; do
              RETRY_COUNT=$((RETRY_COUNT+1))
              echo "⚠️ Push failed, retrying ($RETRY_COUNT/$MAX_RETRIES)..."
              sleep 5
            done
            
            if [ $RETRY_COUNT -eq $MAX_RETRIES ]; then
              echo "❌ Failed to push after $MAX_RETRIES attempts"
              echo "🔒 Privacy policy backup preserved in /tmp/"
              exit 1
            fi
            
            echo "✅ Deployment successful!"
          fi
      
      - name: Summary
        run: |
          echo "## 🚀 Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ Landing page deployed successfully" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📍 Live URLs" >> $GITHUB_STEP_SUMMARY
          echo "- **Landing Page:** https://dhruvinrsoni.github.io/smruti-cortex/" >> $GITHUB_STEP_SUMMARY
          echo "- **Privacy Policy:** https://dhruvinrsoni.github.io/smruti-cortex/privacy.html" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📁 Deployed Files" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          ls -la docs/ >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "🔒 Privacy policy preserved and verified" >> $GITHUB_STEP_SUMMARY
