<!-- TODO before submitting v9.3.0 (delete this block when done):
- [ ] Fill in "Submitted" date after upload.
- [ ] What's New: 3 bullets max, user-facing language.
- [ ] Section 4 reflects manifest.json (run `npm run store check`).
- [ ] Section 7 rewritten from the raw git log below into prose.
- [ ] Screenshots refreshed if the UI changed.

  PERMISSION DELTA vs v9.2.0: NONE.
  Section 4 entries should be byte-identical to v9.2.0.

  Raw git log v9.2.0..v9.3.0:
    - chore(test): lower vitest coverage hard floor to 80/80/80/80
    - chore(deps): npm audit fix — resolve 6 dev-dep advisories
    - chore(deps): narrow dependabot to security-only updates
    - fix(ci): point build.yml package step at release/zips/ path
    - docs: ranking-report triage playbook (maintainer + reporter)
    - feat: report-button kill switch + 5/24h client-side rate limit
    - ci: triage, stale, and dedupe workflows for ranking-bug issues
    - chore(labels): add ranking-bug, auto-report, sink, duplicate? labels
    - feat(report): enrich title with sort= and add degeneracy hint
    - fix(report): tighten typos and phrasing in ranking-report surface
    - feat(report): use Issue Form template for ranking-bug URLs
    - test(e2e): palette row data-url/data-title contract for popup modes
    - feat(search): polar dedup pass keyed on hostname + normalized title
    - fix(sort): trust engine tier order in UI; align ranking-report legend
    - fix(popup): palette-aware Ctrl+C/Ctrl+M for tabs, bookmarks, websearch
    - fix(quick-search): palette-aware Ctrl+C/Ctrl+M and Enter across all prefix modes
    - docs: update latest quality report [skip ci]
    - docs: indexing skill for the three-tier Recent-view freshness model
    - test(e2e): recent-view freshness specs (B5/B6/B7)
    - feat(handlers): live-merge fallback in GET_RECENT_HISTORY
    - fix(background): fast-path single-row IDB upsert on chrome.history.onVisited
    - fix(popup): re-fetch Recent list after a successful Index Now
    - fix(handlers): MANUAL_INDEX clears the recent-history session warm cache
    - refactor(store-ux): swift submission UX (init preamble + check + ship banner)
    - feat(store-check): CHANGELOG entry content check (header-only is FAIL)
    - feat(strip-metadata): --dry-run, --quiet, and protected-dir guard
    - feat(store-check): formalize `store check --json` schema (schemaVersion: 1)
    - feat(store-check): host_permissions parity (manifest <-> submission doc)
    - feat(store-check): Submitted date sanity (future fails, >365d warns)
    - feat(store-check): `store init --force` overwrite is loud + sized
    - feat(store-check): add `store init --dry-run` (preview without writing)
    - feat(store-check): --strict promotes WARN outcomes to FAIL
    - feat(store-check): add version parity check (manifest vs package)
    - fix(store-check): runSilent surfaces stderr tail on failure
    - fix(store-check): cap fetchStore() at 10s via AbortSignal.timeout
    - refactor(sync-version): semver validation, atomic write, and --check mode
    - docs(coverage): align ratchet docs with the new tiered model
    - refactor(ratchet): replace delta baseline with absolute tiered floors (70/80/90)
    - feat(verify,release): --strict flag promotes WARN to FAIL
    - feat(verify,release): --json structured log mode (NDJSON on stdout)
    - feat(release): `npm run ship resume` — pick up an interrupted ship
    - feat(benchmark): soft baseline ratchet + --update-baseline flag
    - refactor(store-check): harden --init scaffolder
    - feat(verify): capture step output, replay tail of failures in summary
    - feat(release): include categorized changelog excerpt in commit body
    - refactor(verify): --no-network downgrades store check, doesn't skip it
    - feat(release): reject `ship check --skip-e2e`
    - refactor(release): strict argv parser with explicit allow-lists
    - refactor(release): full revert cleanup on Step 5/6 failure
    - feat(release): verify gh auth status during prereqs
    - refactor(verify): rename prev-tag check + add next-bump collision check
    - feat(release): pre-bump tag-collision guard in Step 1
    - refactor(release): push main + tag atomically
    - refactor(release): fetch remote tags during prereqs (soft-fail)
    - fix(scripts): write release zip to canonical release/zips/ path
    - docs: D1 sweep for scripts and npm-script refactor
    - refactor(scripts): C — delete orphan scripts (Phase C, audit + cleanup)
    - refactor(scripts): B2 — add five new ship check phases to verify.mjs --release
    - refactor(scripts): B1 — fold preflight.mjs into verify.mjs --release
    - refactor(scripts): A6 — drop unused lifecycle hooks (preversion, version, postversion)
    - refactor(scripts): A5 — ship subcommand dispatcher (patch, minor, major, check)
    - refactor(scripts): A4 — store dispatcher (store check, store init, store unpack)
    - refactor(scripts): A3 — lint dispatcher (lint, lint strict)
    - refactor(scripts): A2 — test dispatcher + e2e/coverage as top-level verbs
    - refactor(scripts): A1 — rename build:prod -> build, drop dev one-shot, start:watch -> dev
    - docs: document Manifest Permission Discipline rule and enforcement layers
    - feat(pre-commit): hard-fail when manifest perms change without store-submission doc edit
    - feat(scripts): scaffolder injects PERMISSION DELTA banner into new submission docs
    - feat(scripts): audit manifest <-> submission-doc permission parity
    - docs(store): simplify v9.2.0 description to LTS append-only minimal copy-paste
    - docs(store): finalize v9.2.0 submission — Submitted date + canonical zip path
    - fix(store-check): support release/zips/ canonical layout
    - docs(store): add idle permission justification + restructure v9.2.0 description
    - docs: update latest quality report [skip ci]
    - fix: repair CHANGELOG heading + archive release notes under docs/release-notes/
    - docs: update latest quality report [skip ci]
-->

<!--
  SCAFFOLDED by scripts/store-check.mjs --init on 2026-04-23.
  TODO before submission:
    1. Fill in Submitted date once uploaded.
    2. Review Section 7 (Changes from Previous Submission) — the git log
       below is a raw dump; rewrite into categorised prose.
    3. If any permission was added/removed, update Section 4.
    4. Delete this comment block.

  Raw git log v9.1.0..v9.2.0:
    - docs: update latest quality report [skip ci]
    - refactor(logging): replace silent catches with DEBUG/WARN breadcrumbs
    - refactor(logging): aggregate hot-path loop failures with per-batch summaries
    - refactor(logging): apply MIXED-policy levels and finish errorMeta rollout
    - refactor(logging): correlate handler logs with message type + add missing error paths
    - refactor(content-scripts): route direct console.* through structured log + preserve full Error objects
    - refactor(popup): route all direct console.* calls through component Logger
    - refactor(settings): migrate legacy Logger.* calls + downgrade setSetting to DEBUG
    - refactor(database): migrate to module-level component Logger
    - fix: correct setForceRebuildFlag Logger call shape
    - chore: remove repository blocklist scanner and all related artifacts
    - fix: sliding-window port rate limiter with 60/s cap
    - feat: client-side dispatch guard collapses duplicate SEARCH_QUERY sends
    - fix: aggregate port rate-limit log into one entry per window
    - fix(popup,quick-search): keep Report button as stable DOM node (toggle hidden instead of remove/recreate)
    - chore(ci): graduate coverage ratchet (soft ±1%, hard beyond, strict opt-in)
    - docs(changelog): backfill SHA for search-core-boundary-flex-v1
    - chore(governance): CODEOWNERS for search-core + CHANGELOG high-impact entry (A6)
    - docs(search-core): boundary-flex contract ADR + VIVEK + skill update (A5)
    - test(e2e): end-to-end lock for boundary-flex ranking (A4)
    - feat(ranking-report): field-hit map + partial-match banner
    - feat(search-core): wire boundary-flex into haystack inclusion gates
    - feat(search-core)!: boundary-flex matching at letter-digit transitions
    - test(search-core): lock current classifyMatch behaviour with golden suite
    - ci(blocklist): install hashed-term blocklist guard (pre-commit + GitHub Action)
    - docs: scrub company-specific references from user-facing docs
    - docs: scrub company-specific references from source doc-comments
    - test: scrub company-specific references from test fixtures
    - docs: report chooser modal + masking gradient contract
    - test(e2e): report chooser + clipboard round-trip
    - feat(quick-search): report chooser modal parity with popup
    - feat(popup): report chooser modal + staged visual cue
    - feat(shared): add report-chooser-utils (options, timings, waitRemaining)
    - fix(masker): align partial/full gradient with pasted-report view
    - chore(e2e): launch Playwright Chromium maximized with null viewport
    - docs: document the Semantic chip and the `requires` pattern for future chips
    - test(e2e): cover Semantic chip disabled/enabled flow with 3 Playwright specs
    - feat: add opt-in Semantic toolbar chip with prerequisite-gated disabled state
    - test(e2e): add Playwright spec for warm-cache recent-history paint
    - feat(mv3): battery-aware keep-alive with adaptive alarm cadence
    - feat(quick-search): warm-cache recent history in chrome.storage.session for instant first paint
    - refactor: centralize sendMessage retry/wake logic into shared helper
    - feat(mv3): re-init service worker on chrome.idle active transitions
    - fix(quick-search): force-wake PING, stale-port detection, exponential retry for hibernate recovery
    - fix(mv3): port messaging calls ensureReady() and responds to PING before init gate
    - fix(embeddings): pause processor with exponential backoff when Ollama or model is unavailable
    - fix(ollama): cache negative availability and dedupe model-missing logs
    - fix(ollama): canonicalize model ids so tag-less names match :latest and other equivalent spellings
    - refactor(ollama): centralize model constants in src/shared/ollama-models.ts
    - fix(popup): deduplicate concurrent recent-history renders
    - fix(embeddings): refuse empty input at every layer
    - docs: update latest quality report [skip ci]
    - Merge pull request #40 from dhruvinrsoni/refactor/coverage-95-solid
    - chore: remove unused eslint-disable directives and dead imports
    - chore: ratchet coverage thresholds to 95/90/95/95
    - test(*): close remaining coverage gaps to hit 95/90/95/95 targets
    - test(handlers,lifecycle): cover diagnostics, settings, commands-listener, and port-messaging
    - test(background): close coverage gaps in indexing, database, and search-engine
    - test(background): cover MessageHandlerRegistry, composition root, and SystemClock
    - refactor(background): extract lifecycle modules (omnibox, port-messaging, commands-listener)
    - fix(ollama): normalize embedding model names to prevent false model-change detections
    - refactor(background): extract 83 message handlers into domain-specific modules
    - refactor(background): introduce MessageHandlerRegistry and composition root
    - refactor(background): extract Port interfaces for all I/O boundaries
    - refactor(core): introduce Result<T, E> type and combinators
    - test(ai-keyword-expander,ollama-service): cover streaming body, code-block parsing, circuit breaker exports, and abort paths
    - test(web-search,palette-messages,tour,helpers): cover untested functions, branch gaps, and edge cases
    - test(settings,logger,traced): cover validation, migration, error paths, and branch gaps
    - test(service-worker): characterize message dispatch for all known message types
    - chore: install coverage ratchet, SOLID skills, and stricter lint rules
    - docs: update latest quality report [skip ci]
    - docs: reorganize CLAUDE.md commands into Develop/Test/Ship categories
    - refactor(logger): slim high-traffic fat-object dumps
    - fix(logger): wrap error args with errorMeta() across callsites
    - fix(logger): pass data/error as separate console args; add errorMeta helper
    - chore(scripts): add strip-metadata helper for unpacking CWS zips
    - fix(quick-search): downgrade spinner safety-timeout log to debug
    - fix(quick-search): downgrade port rate-limit logging to debug on both ends
    - docs: document verify -> preflight -> ship hierarchy
    - refactor(ship): delegate verify gate to npm run preflight
    - refactor(preflight): remove zip write, become read-only
    - refactor(verify): drop build:dev phase
    - docs: simplify README + SKILL into one-page command cheat sheet
    - chore(ci): add Dependabot config for weekly npm + monthly actions
    - chore(ci): minimize staging.yml to build+upload with revive docs
    - chore(ci): archive docker-build.yml with revive instructions
    - feat(release): add npm run ship alias
    - refactor(release): verify-first ordering + auto-scaffold + --skip-e2e
    - refactor(ci): slim pre-commit hook to ~20s
    - feat(release): add store-check.mjs — verify CWS state + scaffold submission docs
    - docs: backfill v9.1.0 CHANGELOG entry
    - docs: add Chrome Web Store submission record for v9.1.0 + backfill checklist
    - docs: update latest quality report [skip ci]
    - chore: Delete extra Documentation
    - docs: add coverage batch1 note
    - test: add extra tests for settings and palette-messages (batch 1)
    - test: add traced tests for traced.ts
    - docs: add instructions for publishing local coverage HTML
    - docs: update latest quality report [skip ci]
    - chore: add local helper to publish coverage HTML (npm publish:coverage)
    - docs: publish coverage HTML to docs/quality-report/coverage [skip ci]
    - docs: update latest quality report [skip ci]
    - ci: publish coverage HTML to docs/quality-report in nfr-report workflow
    - docs: update latest quality report [skip ci]
    - chore: add scripts/show-low-coverage.js (atomic)
    - test: update src/core/__tests__/traced.test.ts (atomic)
    - test: update src/shared/__tests__/palette-messages.test.ts (atomic)
    - docs: update latest quality report [skip ci]
    - fix: add lcov and json-summary coverage reporters to vitest config
    - docs: update latest quality report [skip ci]
-->

# Chrome Web Store Submission — SmrutiCortex v9.3.0

> Version: 9.3.0
> Released (tagged): 2026-04-30
> Drafted: 2026-04-30
> Submitted: _TBD — fill in after upload_
> Package: `release/zips/smruti-cortex-v9.3.0.zip`
> Dashboard: https://chrome.google.com/webstore/devconsole
> Auto-publish: ON
> Previous version: v9.2.0

**Reviewer fast-path:** ONE new required permission vs v9.1.0 — `idle` — used solely as a wake signal to re-initialize the MV3 service worker before the user presses the keyboard shortcut. No new host permissions, no new optional permissions, no new data collection. All other justifications are byte-identical to v9.1.0. This release ships boundary-flex search matching, warm-cache popup paint, opt-in Semantic toolbar chip, and MV3 reliability hardening (idle-resume re-init, battery-aware keep-alive). All processing remains local; all data remains in IndexedDB.

---

## 1. Product Details

### Title

SmrutiCortex: Instant Browser History Search

### Summary (132 chars)

Instant browser history search. Local AI via Ollama: live synonym expansion, per-keyword cache. 100% private. Zero cloud.

### Category

Productivity

### Language

English

### Description

Copy the contents of the fenced block below verbatim into the Chrome Web Store dashboard's "Description" field. Nothing outside the fence is part of the listing.

When a new release ships, **append one line at the end of `RECENT UPDATES:`** inside the fence. Same rule everywhere in this fence: append at end. Do not reorder, do not introduce new section markers, do not maintain a latest-on-top changelog (the CWS dashboard's own "Changes in this version" field already covers that per release).

```text
SmrutiCortex — Your private, lightning-fast browser memory search engine.

Tired of losing important pages in your browser history? SmrutiCortex indexes everything you visit and retrieves any URL in milliseconds. Think "Everything" search for Windows, but for your browser.

KEY FEATURES:

⚡ Ultra-Fast Search — Results appear as you type (< 50ms)
🎯 Smart Ranking — Recent + frequent results with literal substring boost
🎯 Strict Matching — Only shows results containing your search terms (default ON, configurable)
🎲 Diverse Results — Filters duplicate URLs automatically for variety (default ON, configurable)
⭐ Bookmark Search — Search bookmarks alongside history with ★ indicator
🔍 Query Expansion — Find related terms with synonym matching
🖼️ Favicon Caching — Local cache for fast icon loading
📊 Performance Monitor — Real-time search metrics and timing
🔧 Advanced Diagnostics — Export system info for bug reports
🔐 100% Private — All data stays local in IndexedDB
🛡️ Self-Healing — Auto-recovery from errors with retry logic and unified Troubleshooter
🔧 Privacy Controls — Favicon toggle, sensitive-site blacklist
💾 Data Management — Storage quota monitoring, one-click rebuild
⌨️ Keyboard-First — Ctrl+Shift+S instant access, full arrow key navigation
🎨 Clean UI — Minimal, distraction-free interface
🤖 AI Search — Optional local keyword expansion (Ollama)
🌐 Cross-Browser — Works on Chrome, Edge, and Firefox
🎓 Guided Onboarding — In-extension spotlight tour for new users
🔘 Toggle Chip Bar — Quick-toggle settings without opening settings modal
🔔 Toast Notifications — Hover-to-pause, selectable text for error messages
🎨 Theme Support — Dark, Light, or Auto (follows system preference)
📦 Data Portability — Export/import your entire search index
⎋ Smart Escape — First Esc clears input, second Esc closes
🔄 Zero-Reload Updates — Keyboard shortcut survives extension updates
🎛️ Command Palette — Universal browser control with prefix modes (/, >, @, #, ??, ?)
🌍 Web Search — Search Google, YouTube, GitHub, and more from the palette
🪟 Browser Commands — 45+ tab/window management commands (opt-in)
🔖 Recently Closed — Find and restore recently closed tabs from tab switcher

PRIVACY GUARANTEE:

• All data stored locally on your device
• No cloud sync, no external servers
• No analytics, no tracking
• Optional favicon loading (configurable)
• Sensitive-site blacklist (banks, password managers)
• Source-available — inspect the code anytime (BSL-1.1, converts to Apache-2.0 in 2030)
• You can delete all data anytime

HOW IT WORKS:

1. Install extension
2. Browse normally — SmrutiCortex indexes your history automatically
3. Press Ctrl+Shift+S (or click icon) to search
4. Type anything — title, URL, keywords
5. Hit Enter to open

KEYBOARD SHORTCUTS:

• Ctrl+Shift+S: Open search
• Enter: Open result
• Ctrl+Enter: Open in new tab
• Shift+Enter: Open in background tab
• Arrow keys: Navigate results
• Esc: Clear search (first press), close (second press)
• Type "sc " in address bar for quick access

PREFIX MODES (in quick-search overlay):

• / — Everyday commands (sort, theme, toggles)
• > — Power commands (tab management, groups)
• @ — Switch to tab by title + recently closed tabs
• # — Search bookmarks
• ?? — Web search (Google, YouTube, GitHub, etc.)
• ? — Help and discovery

TECHNICAL DETAILS:

• Manifest V3 (future-proof)
• IndexedDB for fast local storage
• Modular scoring algorithms for intelligent ranking
• Service worker background processing (sync-registered listener for cold-start reliability + idle-resume re-init)
• AOP-inspired tracing framework for end-to-end observability
• TypeScript codebase for reliability
• 2,397 unit tests across 70 files + 70+ E2E tests across 11 Playwright specs, coverage ratchet gated in pre-commit

SOURCE-AVAILABLE (BSL-1.1):

View code, report issues: github.com/dhruvinrsoni/smruti-cortex
Free for personal use. Converts to Apache-2.0 on April 1, 2030.

RECENT UPDATES:

v9.0.0 (Apr 2026) — Command Palette, Recently Closed tabs, AOP-style tracing.
v9.1.0 (Apr 2026) — MV3 cold-start reliability + unified Troubleshooter.
v9.2.0 (Apr 2026) — Boundary-flex search, warm-cache popup, idle-resume re-init, battery-aware keep-alive, opt-in Semantic toolbar chip.
```

---

## 2. Additional Fields

| Field | Value |
|-------|-------|
| Official URL | None |
| Homepage URL | https://dhruvinrsoni.github.io/smruti-cortex/ |
| Support URL | https://github.com/dhruvinrsoni/smruti-cortex/issues |
| Mature content | No |

---

## 3. Single Purpose

**Single purpose description:**

Search your browser history instantly with privacy-first local processing.

---

## 4. Permission Justifications

**Delta vs v9.1.0:** ONE new required permission added — `idle` — see its dedicated entry below. All other justifications are byte-identical to v9.1.0 (which were themselves byte-identical to v9.0.0). The full set is reproduced below for reviewer convenience.

> **Maintenance note (do not paste into CWS):** entries below are listed in manifest declaration order (history → bookmarks → storage → tabs → alarms → scripting → activeTab → sessions → windows → idle). When a new permission is added in a future release, append a `#### \`<perm>\`` block at the bottom of the **Required Permissions** section (or **Optional Permissions** section) and update this preamble to flag the delta. Never rewrite existing justifications — they are CWS-approved text and should be preserved verbatim across releases unless the underlying API usage genuinely changes.

### Required Permissions

#### `history`

Core functionality. SmrutiCortex indexes browser history to provide instant search. Uses `chrome.history.search()` to query history entries and `chrome.history.onVisited` to index new visits in real time as the user browses.

#### `bookmarks`

Enables unified search across history and bookmarks. Uses `chrome.bookmarks.getTree()` to index bookmarks alongside history so users can find any saved page from a single search box.

#### `storage`

Persists user preferences and extension state locally. Uses `chrome.storage.local` to store settings (search options, AI configuration, display preferences), cached favicon data, recent search queries, and performance metrics. All data stays on device.

#### `tabs`

Opens search results in browser tabs and communicates with the quick-search overlay. Uses `chrome.tabs.query()` to find the active tab, `chrome.tabs.create()` to open results, `chrome.tabs.sendMessage()` to communicate with the content script overlay, and `chrome.tabs.update()` to switch to existing tabs matching search results.

#### `alarms`

Keeps the MV3 service worker responsive. Uses `chrome.alarms.create()` for periodic keepalive pings that prevent cold-start delays when the user presses the keyboard shortcut. Without this, the first search after idle would be slow because Chrome terminates inactive service workers.

#### `scripting`

Re-injects the quick-search content script after Chrome auto-updates the extension. When Chrome updates an extension, all previously injected content scripts are invalidated — the keyboard shortcut (Ctrl+Shift+S) stops working on already-open tabs. Uses `chrome.scripting.executeScript()` to re-inject ONLY our own content script (`quick-search.js`) into the active tab so the shortcut resumes without requiring a page reload. Never executes arbitrary or remote code. Only triggered when the user presses the keyboard shortcut.

#### `activeTab`

Grants temporary host permission for ONLY the current active tab when the user presses the keyboard shortcut (Ctrl+Shift+S). This temporary permission is required by `chrome.scripting.executeScript()` to re-inject our content script after an extension update. No data is read from the page. Permission expires immediately when the user navigates away or switches tabs. Strictly user-initiated — never granted automatically.

#### `sessions`

Displays recently closed tabs in the tab-switcher mode (`@` prefix) so users can find and restore pages they accidentally closed. Uses `chrome.sessions.getRecentlyClosed()` to list up to 10 recently closed tabs and `chrome.sessions.restore()` to reopen a selected tab. This directly extends the extension's core purpose of helping users find pages in their browsing history — recently closed tabs are the most common "lost page" scenario.

#### `windows`

Focuses the correct browser window when the user switches to a tab from search results. When a search result belongs to a tab in another window, `chrome.windows.update(windowId, { focused: true })` brings that window to the foreground. Also powers "Move Tab to New Window" and "Merge Windows" command palette actions. All uses are strictly user-initiated.

#### `idle` *(new in v9.2.0)*

Wakes the MV3 service worker when the user returns to the browser after being away, so the very first search after idle is fast. Uses `chrome.idle.onStateChanged` to listen for the `idle → active` transition and proactively re-initializes the service worker (re-arms message handlers, port listeners, and the search index) before the user presses the keyboard shortcut. Without this signal, Chrome's MV3 hibernation policy can leave the service worker in a half-initialized state for the first interaction after a long idle, producing a visible cold-start delay.

This permission grants ONLY the user's session state (active / idle / locked) — not browser activity, not page content, not input. The state is consumed in-process as a wake signal and is never logged, persisted, or transmitted. Combined with the existing `alarms`-based keep-alive, this completes the extension's MV3 reliability story across both short hibernation (alarms) and long idle (idle event).

### Optional Permissions

These three permissions are declared as `optional_permissions` in the manifest. They are never granted at install time. They are only requested at runtime via `chrome.permissions.request()` when the user explicitly enables the "Advanced Browser Commands" setting. If the user denies the permission prompt, the feature stays off and these APIs are never called.

#### `tabGroups`

Powers command palette actions to create, collapse, expand, name, and color tab groups. Every call to `chrome.tabGroups.query()` and `chrome.tabGroups.update()` is gated behind a `chrome.permissions.contains()` check that verifies the permission was granted. If the user never enables Advanced Browser Commands, this API is never invoked.

#### `browsingData`

Powers command palette actions to clear browser cache, cookies, local storage, form data, downloads history, and saved passwords. Every call to `chrome.browsingData.removeCache()`, `chrome.browsingData.removeCookies()`, and related methods is gated behind a `chrome.permissions.contains()` check. If the user never enables Advanced Browser Commands, this API is never invoked.

#### `topSites`

Shows the browser's most-visited sites in the command palette. Uses `chrome.topSites.get()` to retrieve the list, gated behind a `chrome.permissions.contains()` check. If the user never enables Advanced Browser Commands, this API is never invoked.

### Optional Host Permissions

#### `<all_urls>`

Declared as `optional_host_permissions`. Used in two ways: (1) `content_scripts` match pattern to inject the quick-search overlay on any webpage when the user presses the keyboard shortcut (Ctrl+Shift+S), and (2) optional favicon fetching from Google's favicon API for display purposes only. No page content is collected, transmitted, or stored. All data remains on the user's device.

---

## 5. Remote Code

**Are you using remote code?** No

**Justification:**

All extension code is bundled in the submitted package. No code is fetched from or executed from remote servers. The optional local AI feature communicates with Ollama, which is a separate application the user installs and runs on their own machine — this is local inter-process communication, not remote code execution.

---

## 6. Data Usage

### Data types collected

| Data Type | Collected? | Justification |
|-----------|------------|---------------|
| Personally identifiable information | No | — |
| Health information | No | — |
| Financial and payment information | No | — |
| Authentication information | No | — |
| Personal communications | No | — |
| Location | No | — |
| **Web history** | **Yes** | Core feature — indexes visited page titles, URLs, and visit timestamps locally in IndexedDB for instant search. Data never leaves the device. No data is transmitted to any server. |
| User activity | No | — |
| Website content | No | — |

### Certifications

- [x] I do not sell or transfer user data to third parties, apart from the approved use cases
- [x] I do not use or transfer user data for purposes that are unrelated to my item's single purpose
- [x] I do not use or transfer user data to determine creditworthiness or for lending purposes

### Privacy Policy

https://dhruvinrsoni.github.io/smruti-cortex/privacy.html

---

## 7. Changes from Previous Submission

### New in v9.2.0 (vs v9.1.0) — Search Quality + MV3 Reliability + Opt-In Semantic

This release pairs a meaningful search-quality improvement (boundary-flex matching) with deeper MV3 reliability hardening and one new opt-in feature surface (Semantic toolbar chip). It adds **one** new required permission — `idle` — used solely as a service-worker wake signal. No new host permissions, no new optional permissions, no expansion of data collection.

**Search quality (user-facing):**
- **Boundary-flex matching (`search-core-boundary-flex-v1`)** — the search core now treats common token boundaries (space, hyphen, underscore, dot, slash) as soft separators, so `module42` finds `Module 42`, `Module-42`, `module_42`, `module.42`, `module/42`. Clean exact matches still rank above flex matches; ranking remains deterministic and locally computed.
- **Warm-cache popup** — recent-history snapshot is cached so the popup paints instantly on first open.

**MV3 reliability (user-facing as "fewer cold-start delays"):**
- **`idle`-based service-worker re-init** — listens for `chrome.idle.onStateChanged` active transitions to re-arm the search index, port listeners, and message handlers before the user presses the shortcut. *(This is the one new permission — see Section 4.)*
- **Battery-aware keep-alive** — adaptive `chrome.alarms` cadence that backs off when on battery and resumes normal cadence when on AC, reducing power drain without sacrificing first-search latency.
- **Hardened Ollama integration** — exponential backoff when the local Ollama service is unreachable, plus model-name canonicalization (`llama3` ↔ `llama3:latest`) so user-configured names match what Ollama actually serves.

**Opt-in surfaces:**
- **Semantic toolbar chip** — a new opt-in toolbar chip exposes the embedding-based semantic search path. Off by default. Prerequisite-gated: only appears once embeddings are configured and ready. No background processing or network calls happen unless the user enables it.
- **Report Chooser modal** — three-option chooser (system info / search trace / full diagnostic) for filing better bug reports from the diagnostics panel.

**Observability + structured logging (developer-facing, zero runtime impact for users):**
- LTS logging policy applied repo-wide — `INFO`/`WARN`/`ERROR`/`DEBUG` levels normalized; per-item recoverable failures downgraded to `DEBUG` with batch-level `WARN` summaries; silent `.catch()` blocks replaced with explicit logs throughout `service-worker`, `embedding-processor`, `indexing`, `commands-listener`, and `recent-history-cache`.
- Component-scoped loggers (`Logger.forComponent('Name')`) consistent across new modules.

**Release infrastructure (no runtime impact for users):**
- New `release/zips/` and `release/notes/` subdirectories + permanent `docs/release-notes/` archive — release artifacts now have a clean, versioned home.
- `scripts/store-check.mjs --init` scaffolds a new submission doc by inheriting the previous version's content (the preamble + structured description in this very doc are now the durable template).

**Test suite:** 2,397 unit tests across 70 files + 70+ E2E Playwright tests across 11 specs (up from 1,233 / 46 / 45 / 7 in v9.1.0). Coverage ratchet enforced in pre-commit.

**What reviewers do NOT need to re-check:**
- Optional permissions — byte-identical to v9.1.0 (`tabGroups`, `browsingData`, `topSites`, `<all_urls>` host)
- Existing required permissions — byte-identical justifications for `history`, `bookmarks`, `storage`, `tabs`, `alarms`, `scripting`, `activeTab`, `sessions`, `windows`
- Data collection — no change, still only local Web history in IndexedDB
- Remote code — no change, still none (Ollama remains local IPC, not remote code)
- Single purpose — no change

---

## 8. Reviewer Notes

This release adds search-quality and MV3 reliability improvements on top of the v9.1.0 foundation. The single new permission (`idle`) is used purely as a wake signal and does not unlock any new data-access surface — see Section 4 for full justification.

All v9.0.0 / v9.1.0 permission justifications, data-usage declarations, and single-purpose framing remain accurate and unchanged. If you reviewed v9.1.0, the only delta requiring fresh attention is the `idle` permission entry in Section 4 plus the `[ WHAT'S NEW IN v9.2.0 ]` block in the description.

Full source is available at https://github.com/dhruvinrsoni/smruti-cortex under BSL-1.1.

---

## 9. Submission Checklist

- [ ] Upload `release/zips/smruti-cortex-v9.2.0.zip` to Chrome Web Store dashboard
- [ ] Paste Description text into "Description" field (replaces v9.1.0 description — adds the new structured `═══ [ SECTION ] ═══` markers and the `[ WHAT'S NEW IN v9.2.0 ]` block)
- [ ] Paste Summary into "Summary" field (unchanged from v9.1.0)
- [ ] Paste Single Purpose description (unchanged from v9.1.0)
- [ ] **Add NEW permission justification for `idle`** — paste the entry from Section 4 into the per-permission justification field on the dashboard
- [ ] Verify all OTHER permission justifications are unchanged from v9.1.0 (byte-identical)
- [ ] Verify "Web history" is checked under Data Usage
- [ ] Verify all three data usage certifications are checked
- [ ] Set privacy policy URL
- [ ] Paste "Changes in this version" — short variant of Section 7 (3-5 bullet recap of boundary-flex, warm-cache, idle-resume, semantic chip; full prose lives in this doc)
- [ ] Review screenshots — no destructive UI changes in v9.2.0, existing screenshots still accurate; consider adding one screenshot of the optional Semantic toolbar chip if reviewers flag it
- [ ] Set Category to "Productivity"
- [ ] Set auto-publish ON
- [ ] Submit for review
- [ ] After upload: fill in "Submitted" date at the top of this document
- [ ] Run `npm run store check` once the listing reflects v9.2.0 (verifies live store version + "What's New" freshness)
