{
  "name": "smriti-cortex",
  "version": "0.1.0",
  "private": true,
  "description": "SmritiCortex — Everything-like instant browser history search (MV3).",
  "scripts": {
    "clean": "rimraf dist",
    "build:dev": "npm run clean && npm run tsc && node ./scripts/copy-static.mjs && node ./scripts/esbuild-dev.mjs",
    "build": "npm run clean && npm run tsc && node ./scripts/copy-static.mjs && node ./scripts/esbuild-prod.mjs",
    "tsc": "tsc --project tsconfig.json",
    "lint": "echo \"No linter configured\"",
    "start:watch": "node ./scripts/esbuild-dev.mjs --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.188",
    "esbuild": "^0.18.0",
    "rimraf": "^5.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "webextension-polyfill": "^0.10.0"
  }
}